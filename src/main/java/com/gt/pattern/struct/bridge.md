~~~~

桥接模式:
    使用组合，将两个不再同一个层面的高层抽象进行连接
    
    Shape: ---> 聚合Color
        circle | rectangle
    Color:
        red | green
    
    将不同维度的两类分别进行抽象，其中一个抽象聚合另一种抽象，
这样可以使得两种维度分别进行扩展而且达到部分互动，防止牵一发
而动全身。
~~~~